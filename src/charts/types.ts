/** `Type` of the Figma `BarChart` set. `Basic` is `grouped` with one series. */
export type BarChartType = 'grouped' | 'stacked'

/** `Type` of the Figma `LineChart` set. */
export type LineChartType = 'line' | 'area'

/** `Type` of the Figma `PieChart` set. */
export type PieChartType = 'pie' | 'doughnut' | 'doughnut-rounded'

/*
 * Patterns - page-level arrangements of organisms (the template layer of
 * atomic design). A pattern may import every layer below it.
 *
 * There is deliberately no screen layer: this package supplies building
 * blocks, and screens belong to the products that use it.
 *
 * Empty until a pattern is ported from the Figma `Pattern` page.
 */
export {}

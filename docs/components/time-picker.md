---
id: "evoq-ui:time-picker"
name: "EvTimePicker"
tag: "ev-time-picker"
layer: "organism"
package: "evoq-ui"
entry: "evoq-ui"
import: "import { EvTimePicker } from 'evoq-ui'"
source: "src/organisms/time-picker/EvTimePicker.vue"
summarySource: "code"
partOf: null
children: []
composes:
  - "evoq-ui:button"
usedBy:
  - "evoq-ui:input-time"
seeAlso: []
figma:
  file: "hJ3XmwLptPtF4IFBDlZlpx"
  doc: "4218:66637"
  sets:
    - "M - TimePickerPopup (2579:14063)"
    - "M- TimePickerDrawer (2844:6656)"
keywords:
  - "time"
  - "clock"
  - "hour minute"
  - "timepicker"
---

<!-- GENERATED by scripts/build-docs.mjs - edit the sources it names, not this file. -->

# EvTimePicker

> A scrolling wheel for picking a time - 24-hour, with seconds, or AM/PM - with Apply and Cancel.

| Field | Value |
| --- | --- |
| ID | `evoq-ui:time-picker` |
| Layer | Organism (`src/organisms`) |
| Import | `import { EvTimePicker } from 'evoq-ui'` |
| Template tag | `<EvTimePicker>` or `<ev-time-picker>` |
| Source | `src/organisms/time-picker/EvTimePicker.vue` |
| Extra attributes go to | the root `<div class="ev-time-picker">` |

## Example

```vue
<EvTimePicker />
```

_The exact snippet the playground simulator copies for this component (`playground/simulator-demos.ts`)._

## Props (8)

| Prop | Type | Default | Required | Description |
| --- | --- | --- | --- | --- |
| `modelValue` | `string` | `'00:00'` | no | — |
| `format` | `'default'` \| `'with-seconds'` \| `'am-pm'` \| `'am-pm-seconds'` | `'default'` | no | Figma's `Variant`: which columns the wheel shows. |
| `disabled` | `boolean` | `false` | no | — |
| `readonly` | `boolean` | `false` | no | — |
| `hasReset` | `boolean` | `false` | no | Figma's `Has Reset Button`: the refresh glyph beside the value. |
| `resetLabel` | `string` | `'Atur ulang'` | no | — |
| `cancelLabel` | `string` | `'Cancel'` | no | — |
| `applyLabel` | `string` | `'Apply'` | no | — |

**Two-way binding:** `v-model`

## Slots (0)

None.

## Events (4)

| Event | Description |
| --- | --- |
| `update:modelValue` | — |
| `apply` | — |
| `cancel` | — |
| `reset` | — |

## Composition

| Relation | Components |
| --- | --- |
| Built from | `evoq-ui:button` |
| Used by | `evoq-ui:input-time` |
| Slot children | — |

## Design guidance

From the Figma page **Calendar & Time Picker**, frame `4218:66637` (Calendar & Time Picker). These rules are binding. Names in them are Figma's - the Vue API is the tables above.

### When to use

- Capture a date, date range, or time. Use the desktop Calendar for inline selection; on mobile use the Popup for compact fields or the Drawer for full-width bottom-sheet selection.

### When not to use

- A single known date that's faster to type. Avoid a Drawer on desktop or an inline Calendar on small mobile screens — match the picker to the breakpoint.

### Do

- Match the platform — D- for desktop, M- for mobile. Offer presets for common ranges, and provide a Reset to clear the selection.

### Don't

- Don't mix desktop and mobile variants in one breakpoint. Don't hide the selected value. Don't omit AM/PM where 12-hour time is expected.

## Contract

- Only the props, slots and events above exist on `EvTimePicker`; anything not listed is not part of its API.
- An attribute that is not a prop (`class`, `id`, `aria-*`, `data-*`) goes to the root `<div class="ev-time-picker">`.
- Import it by name from `evoq-ui`; styles come once from `evoq-ui/style.css`.
- Refer to it by id `evoq-ui:time-picker` - the id stays the same if the component moves layer.

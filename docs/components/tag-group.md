---
id: "evoq-ui:tag-group"
name: "EvTagGroup"
tag: "ev-tag-group"
layer: "molecule"
package: "evoq-ui"
entry: "evoq-ui"
import: "import { EvTagGroup } from 'evoq-ui'"
source: "src/molecules/tag-group/EvTagGroup.vue"
summarySource: "code"
partOf: null
children:
  - "evoq-ui:tag"
composes: []
usedBy: []
seeAlso:
  - "evoq-ui:badge"
  - "evoq-ui:button"
  - "evoq-ui:tabs"
figma:
  file: "hJ3XmwLptPtF4IFBDlZlpx"
  doc: "4105:63441"
  sets:
    - "TagGroup (3050:6644)"
keywords:
  - "chips"
  - "tag list"
  - "filters"
---

<!-- GENERATED by scripts/build-docs.mjs - edit the sources it names, not this file. -->

# EvTagGroup

> Lays out a set of tags, wrapping or scrolling, and hands them one shared variant.

| Field | Value |
| --- | --- |
| ID | `evoq-ui:tag-group` |
| Layer | Molecule (`src/molecules`) |
| Import | `import { EvTagGroup } from 'evoq-ui'` |
| Template tag | `<EvTagGroup>` or `<ev-tag-group>` |
| Source | `src/molecules/tag-group/EvTagGroup.vue` |
| Extra attributes go to | the root `<div class="ev-tag-group">` |

## Example

```vue
<EvTagGroup>
  <EvTag removable>Pengadaan</EvTag>
  <EvTag removable>Q1 2026</EvTag>
  <EvTag removable>Prioritas tinggi</EvTag>
</EvTagGroup>
```

_The exact snippet the playground simulator copies for this component (`playground/simulator-demos.ts`)._

Its default slot is built to hold `EvTag` (`evoq-ui:tag`).

## Props (4)

| Prop | Type | Default | Required | Description |
| --- | --- | --- | --- | --- |
| `variant` | `'default'` \| `'outline'` | — | no | Cascades to every `EvTag` inside that does not set its own. |
| `spacing` | `'default'` \| `'loose'` | `'default'` | no | Figma's `Spacing`: 4px (`default`) or 8px (`loose`). |
| `type` | `'wrap'` \| `'scroll'` | `'wrap'` | no | Figma's `Type`: wrap onto more lines, or one line that scrolls. |
| `label` | `string` | — | no | Accessible name for the group. |

## Slots (1)

| Slot | Description |
| --- | --- |
| `default` | — |

## Events (0)

None.

## Composition

| Relation | Components |
| --- | --- |
| Built from | — (renders no other Ev component) |
| Used by | — |
| Slot children | `evoq-ui:tag` |

## Design guidance

From the Figma page **Tag**, frame `4105:63441` (Tag). These rules are binding. Names in them are Figma's - the Vue API is the tables above.

### When to use

- Use to label an item with a keyword, category, status, or attribute.
- Use a removable tag (with close) for user-applied filters or selections.
- Use a leading icon to reinforce the tag's type or meaning.
- Use the outline style for a lighter, lower-emphasis label.

### When not to use

- Don't use to trigger an action  →  use Button (`evoq-ui:button`)
- Don't use for a standalone status  →  use Badge (`evoq-ui:badge`)
- Don't use for navigation between views  →  use Tabs (`evoq-ui:tabs`)
- Don't use long phrases or sentences as a tag.

### Do

- Keep labels short (1–2 words), sentence case
- Show the close icon only when the tag is removable
- Group related tags and let them wrap
- Use a consistent variant within one group

### Don't

- Pack a long sentence into a tag
- Mix removable and static tags without a cue
- Use color alone to convey meaning
- Use tags as primary navigation

## Contract

- Only the props, slots and events above exist on `EvTagGroup`; anything not listed is not part of its API.
- An attribute that is not a prop (`class`, `id`, `aria-*`, `data-*`) goes to the root `<div class="ev-tag-group">`.
- Import it by name from `evoq-ui`; styles come once from `evoq-ui/style.css`.
- Refer to it by id `evoq-ui:tag-group` - the id stays the same if the component moves layer.

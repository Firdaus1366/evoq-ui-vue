---
id: "evoq-ui:avatar"
name: "EvAvatar"
tag: "ev-avatar"
layer: "atom"
package: "evoq-ui"
entry: "evoq-ui"
import: "import { EvAvatar } from 'evoq-ui'"
source: "src/atoms/avatar/EvAvatar.vue"
summarySource: "figma"
partOf: null
children: []
composes: []
usedBy: []
seeAlso:
  - "evoq-ui:badge"
  - "evoq-ui:button"
figma:
  file: "hJ3XmwLptPtF4IFBDlZlpx"
  doc: "3715:14719"
  sets:
    - "Avatar (497:979)"
keywords:
  - "user"
  - "profile"
  - "initials"
  - "photo"
  - "person"
---

<!-- GENERATED by scripts/build-docs.mjs - edit the sources it names, not this file. -->

# EvAvatar

> An avatar represents a user or entity through an image, initials, or icon, helping provide identity and context within the interface.

| Field | Value |
| --- | --- |
| ID | `evoq-ui:avatar` |
| Layer | Atom (`src/atoms`) |
| Import | `import { EvAvatar } from 'evoq-ui'` |
| Template tag | `<EvAvatar>` or `<ev-avatar>` |
| Source | `src/atoms/avatar/EvAvatar.vue` |
| Extra attributes go to | the root `<span class="ev-avatar">` |

## Example

```vue
<EvAvatar
  label="AB"
  alt="Anya Bahar"
/>
```

_The exact snippet the playground simulator copies for this component (`playground/simulator-demos.ts`)._

## Props (5)

| Prop | Type | Default | Required | Description |
| --- | --- | --- | --- | --- |
| `size` | `24` \| `32` \| `40` \| `48` \| `64` \| `96` | `32` | no | — |
| `variant` | `'green'` \| `'blue'` \| `'orange'` \| `'purple'` \| `'teal'` \| `'pink'` \| `'number'` \| `'empty'` \| `'error'` | `'blue'` | no | Tint of the initials avatar, or one of the three grey states: `number` for an overflow count, `empty` for no person, `error` for a failed image. |
| `label` | `string` | — | no | Initials, or the count for the `number` variant. |
| `src` | `string` | — | no | Shows the photo instead of the initials. |
| `alt` | `string` | — | no | Accessible name. Falls back to the initials. |

## Slots (1)

| Slot | Description |
| --- | --- |
| `default` | Replaces the whole face - use for a custom glyph. |

## Events (0)

None.

## Composition

| Relation | Components |
| --- | --- |
| Built from | — (renders no other Ev component) |
| Used by | — |
| Slot children | — |

## Design guidance

From the Figma page **Avatar**, frame `3715:14719` (Avatar). These rules are binding. Names in them are Figma's - the Vue API is the tables above.

### When to use

- Use to identify the author of a comment, message, post, or activity entry.
- Use in lists, tables, or cards to scan owner/assignee at a glance.
- Use letter variant (initials on colored bg) when no photo is available.
- Use number variant for grouped count indicators (e.g., “+3 more” in a stack).
- Use empty variant when identity is unknown but a placeholder slot is needed.
- Use error variant when an avatar fails to load or a user is invalid.

### When not to use

- Don't use for system/non-human entities  →  use Icon
- Don't use for status indicators alone  →  use Badge (`evoq-ui:badge`)
- Don't use as the only menu trigger  →  use Button (`evoq-ui:button`) as wrapper
- Don't use at sizes below 24px  →  use Icon (initials unreadable)

### Do

- Pick the avatar color deterministically from user id/initials so it stays stable
- Use the face-centered crop for image variant
- Use 1–2 uppercase letters for initials (“AB”)
- Add aria-label describing the user's name

### Don't

- Randomize avatar color on each render
- Use a non-square source image without cropping
- Use 3+ letters or lowercase initials
- Leave avatars unannounced for screen readers

## Contract

- Only the props, slots and events above exist on `EvAvatar`; anything not listed is not part of its API.
- An attribute that is not a prop (`class`, `id`, `aria-*`, `data-*`) goes to the root `<span class="ev-avatar">`.
- Import it by name from `evoq-ui`; styles come once from `evoq-ui/style.css`.
- Refer to it by id `evoq-ui:avatar` - the id stays the same if the component moves layer.

---
id: "evoq-ui:nav-menu-item"
name: "EvNavMenuItem"
tag: "ev-nav-menu-item"
layer: "atom"
package: "evoq-ui"
entry: "evoq-ui"
import: "import { EvNavMenuItem } from 'evoq-ui'"
source: "src/atoms/nav-menu-item/EvNavMenuItem.vue"
summarySource: "code"
partOf: "evoq-ui:navigation-menu"
children: []
composes: []
usedBy: []
seeAlso:
  - "evoq-ui:drawer"
  - "evoq-ui:tabs"
figma:
  file: "hJ3XmwLptPtF4IFBDlZlpx"
  doc: "4347:81532"
  sets:
    - ".TabMenubar (2065:2424)"
keywords:
  - "nav item"
  - "menu link"
  - "top bar item"
---

<!-- GENERATED by scripts/build-docs.mjs - edit the sources it names, not this file. -->

# EvNavMenuItem

> One destination in a NavigationMenu - a link when given an href, marked as the current section when active.

| Field | Value |
| --- | --- |
| ID | `evoq-ui:nav-menu-item` |
| Layer | Atom (`src/atoms`) |
| Import | `import { EvNavMenuItem } from 'evoq-ui'` |
| Template tag | `<EvNavMenuItem>` or `<ev-nav-menu-item>` |
| Source | `src/atoms/nav-menu-item/EvNavMenuItem.vue` |
| Extra attributes go to | the root element `.ev-nav-menu-item` |
| Used inside | `evoq-ui:navigation-menu` (EvNavigationMenu) |

## Example

```vue
<EvNavMenuItem>
  Pengadaan
</EvNavMenuItem>
```

_The exact snippet the playground simulator copies for this component (`playground/simulator-demos.ts`)._

## Props (3)

| Prop | Type | Default | Required | Description |
| --- | --- | --- | --- | --- |
| `active` | `boolean` | `false` | no | Marks the current section. |
| `href` | `string` | — | no | Renders an anchor instead of a button. |
| `iconOnly` | `boolean` | `false` | no | Figma's `Icon Only` state: a 32px square pill. Give it an `aria-label`, since there is no visible text. |

## Slots (2)

| Slot | Description |
| --- | --- |
| `default` | — |
| `icon` | — |

## Events (1)

| Event | Description |
| --- | --- |
| `click` | — |

## Composition

| Relation | Components |
| --- | --- |
| Built from | — (renders no other Ev component) |
| Used by | — |
| Slot children | — |

## Design guidance

From the Figma page **Navigation Menu - Desktop**, frame `4347:81532` (Navigation Menu). These rules are binding. Names in them are Figma's - the Vue API is the tables above.

### When to use

- Use as the persistent top-level navigation for a desktop application — anchoring the brand, exposing primary sections, and housing global utilities like search and the user profile.

### When not to use

- Don't use for in-page section switching — use Tabs (`evoq-ui:tabs`).
- Don't use on mobile layouts — use the mobile navigation pattern (bottom bar or hamburger drawer (`evoq-ui:drawer`)) instead of this desktop bar.

### Do

- Keep navigation labels short and stable.
- Highlight the active section clearly.
- Use Tab Style when sections behave like switchable views, and Menu when they are distinct destinations.

### Don't

- Don't overcrowd the bar with too many top-level links — group secondary items into a menu or slot. Don't hide critical destinations behind search only.

## Contract

- Only the props, slots and events above exist on `EvNavMenuItem`; anything not listed is not part of its API.
- An attribute that is not a prop (`class`, `id`, `aria-*`, `data-*`) goes to the root element `.ev-nav-menu-item`.
- Import it by name from `evoq-ui`; styles come once from `evoq-ui/style.css`.
- Refer to it by id `evoq-ui:nav-menu-item` - the id stays the same if the component moves layer.

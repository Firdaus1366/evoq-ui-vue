---
id: "evoq-ui:button-group-item"
name: "EvButtonGroupItem"
tag: "ev-button-group-item"
layer: "atom"
package: "evoq-ui"
entry: "evoq-ui"
import: "import { EvButtonGroupItem } from 'evoq-ui'"
source: "src/atoms/button-group-item/EvButtonGroupItem.vue"
summarySource: "code"
partOf: "evoq-ui:button-group"
children: []
composes: []
usedBy: []
seeAlso:
  - "evoq-ui:button"
figma:
  file: "hJ3XmwLptPtF4IFBDlZlpx"
  doc: "4187:64907"
  sets:
    - "ButtonGroup Item (749:4128)"
keywords:
  - "segment"
  - "pagination item"
---

<!-- GENERATED by scripts/build-docs.mjs - edit the sources it names, not this file. -->

# EvButtonGroupItem

> One segment of a ButtonGroup - a button that takes its variant and size from the group around it.

| Field | Value |
| --- | --- |
| ID | `evoq-ui:button-group-item` |
| Layer | Atom (`src/atoms`) |
| Import | `import { EvButtonGroupItem } from 'evoq-ui'` |
| Template tag | `<EvButtonGroupItem>` or `<ev-button-group-item>` |
| Source | `src/atoms/button-group-item/EvButtonGroupItem.vue` |
| Extra attributes go to | the root `<button class="ev-button-group__item">` |
| Used inside | `evoq-ui:button-group` (EvButtonGroup) |

## Example

```vue
<EvButtonGroupItem>
  Mingguan
</EvButtonGroupItem>
```

_The exact snippet the playground simulator copies for this component (`playground/simulator-demos.ts`)._

## Props (6)

| Prop | Type | Default | Required | Description |
| --- | --- | --- | --- | --- |
| `variant` | `'default-light'` \| `'default-white'` \| `'primary'` \| `'destructive'` \| `'warning'` | — | no | Overrides the group's variant for this one item. |
| `size` | `'large'` \| `'small'` | — | no | — |
| `active` | `boolean` | `false` | no | Marks this item as the current choice. |
| `disabled` | `boolean` | `false` | no | — |
| `iconOnly` | `boolean` | `false` | no | Square item holding only an icon. Give it an `aria-label`. |
| `type` | `'button'` \| `'submit'` \| `'reset'` | `'button'` | no | — |

## Slots (3)

| Slot | Description |
| --- | --- |
| `default` | — |
| `iconLeft` | — |
| `iconRight` | — |

## Events (1)

| Event | Description |
| --- | --- |
| `click` | — |

## Composition

| Relation | Components |
| --- | --- |
| Built from | — (renders no other Ev component) |
| Used by | — |
| Slot children | — |

## Design guidance

From the Figma page **Button Group**, frame `4187:64907` (Button Group). These rules are binding. Names in them are Figma's - the Vue API is the tables above.

### When to use

- Present a short, fixed set of related choices or page numbers as one connected control — pagination, view toggles, segmented filters.

### When not to use

- A single action → use Button (`evoq-ui:button`). Many pages (>9) → use Pagination with ellipsis. Unrelated actions that shouldn't read as one control.

### Do

- Keep the count small and labels short. Group only related items. Mark the active item as selected. Use 9+ for ranges beyond nine.

### Don't

- Don't mix unrelated actions in one group. Don't exceed nine items — use the 9+ overflow. Don't use it for a page's primary action.

## Contract

- Only the props, slots and events above exist on `EvButtonGroupItem`; anything not listed is not part of its API.
- An attribute that is not a prop (`class`, `id`, `aria-*`, `data-*`) goes to the root `<button class="ev-button-group__item">`.
- Import it by name from `evoq-ui`; styles come once from `evoq-ui/style.css`.
- Refer to it by id `evoq-ui:button-group-item` - the id stays the same if the component moves layer.

---
id: "evoq-ui:nav-menu-mobile-item"
name: "EvNavMenuMobileItem"
tag: "ev-nav-menu-mobile-item"
layer: "atom"
package: "evoq-ui"
entry: "evoq-ui"
import: "import { EvNavMenuMobileItem } from 'evoq-ui'"
source: "src/atoms/nav-menu-mobile-item/EvNavMenuMobileItem.vue"
summarySource: "code"
partOf: "evoq-ui:navigation-menu-mobile"
children: []
composes: []
usedBy: []
seeAlso: []
figma:
  file: "hJ3XmwLptPtF4IFBDlZlpx"
  doc: "4347:82858"
  sets:
    - ".Menu (2559:3036)"
keywords:
  - "tab bar item"
  - "bottom nav item"
---

<!-- GENERATED by scripts/build-docs.mjs - edit the sources it names, not this file. -->

# EvNavMenuMobileItem

> One destination in the mobile navigation bar: a 24px icon over a 12px label, filling its share of the row.

| Field | Value |
| --- | --- |
| ID | `evoq-ui:nav-menu-mobile-item` |
| Layer | Atom (`src/atoms`) |
| Import | `import { EvNavMenuMobileItem } from 'evoq-ui'` |
| Template tag | `<EvNavMenuMobileItem>` or `<ev-nav-menu-mobile-item>` |
| Source | `src/atoms/nav-menu-mobile-item/EvNavMenuMobileItem.vue` |
| Extra attributes go to | the root element `.ev-nav-menu-mobile-item` |
| Used inside | `evoq-ui:navigation-menu-mobile` (EvNavigationMenuMobile) |

## Example

```vue
<EvNavMenuMobileItem
  active
  label="Beranda"
/>
```

_The exact snippet the playground simulator copies for this component (`playground/simulator-demos.ts`)._

## Props (3)

| Prop | Type | Default | Required | Description |
| --- | --- | --- | --- | --- |
| `active` | `boolean` | `false` | no | Marks the current section. Figma models it as the `Active` variant. |
| `label` | `string` | — | no | — |
| `href` | `string` | — | no | Renders an anchor instead of a button. |

## Slots (0)

None.

## Events (1)

| Event | Description |
| --- | --- |
| `click` | — |

## Composition

| Relation | Components |
| --- | --- |
| Built from | — (renders no other Ev component) |
| Used by | — |
| Slot children | — |

## Design guidance

From the Figma page **Navigation Menu - Mobile**, frame `4347:82858` (Navigation Menu (Mobile)). These rules are binding. Names in them are Figma's - the Vue API is the tables above.

### When to use

- Use as the persistent bottom navigation on mobile for 2–5 primary destinations. Choose the Center Button variant when one action (create, scan, post) is clearly the primary task.

### When not to use

- Don't exceed 5 destinations — overflow belongs in a "More" menu. Don't use on desktop — use the desktop navigation bar. Don't put secondary actions in the center button.

### Do

- Keep labels to one word and pair each with a clear icon. Mark the active destination with the brand-blue icon and label. Keep the item count consistent across the app.

### Don't

- Don't hide the bar on scroll for core flows, and don't change the icon set or order between screens. Don't label the center action ambiguously.

## Contract

- Only the props, slots and events above exist on `EvNavMenuMobileItem`; anything not listed is not part of its API.
- An attribute that is not a prop (`class`, `id`, `aria-*`, `data-*`) goes to the root element `.ev-nav-menu-mobile-item`.
- Import it by name from `evoq-ui`; styles come once from `evoq-ui/style.css`.
- Refer to it by id `evoq-ui:nav-menu-mobile-item` - the id stays the same if the component moves layer.

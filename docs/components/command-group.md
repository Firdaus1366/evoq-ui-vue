---
id: "evoq-ui:command-group"
name: "EvCommandGroup"
tag: "ev-command-group"
layer: "organism"
package: "evoq-ui"
entry: "evoq-ui"
import: "import { EvCommandGroup } from 'evoq-ui'"
source: "src/organisms/command/EvCommandGroup.vue"
summarySource: "code"
partOf: "evoq-ui:command"
children:
  - "evoq-ui:command-item"
composes:
  - "evoq-ui:command-item"
usedBy: []
seeAlso: []
figma:
  file: "hJ3XmwLptPtF4IFBDlZlpx"
  doc: "4220:73205"
  sets: []
keywords:
  - "command section"
  - "group heading"
---

<!-- GENERATED by scripts/build-docs.mjs - edit the sources it names, not this file. -->

# EvCommandGroup

> One Section of a Command palette: a muted heading row over its commands, with the rule that separates it from the section above.

| Field | Value |
| --- | --- |
| ID | `evoq-ui:command-group` |
| Layer | Organism (`src/organisms`) |
| Import | `import { EvCommandGroup } from 'evoq-ui'` |
| Template tag | `<EvCommandGroup>` or `<ev-command-group>` |
| Source | `src/organisms/command/EvCommandGroup.vue` |
| Extra attributes go to | the root `<div class="ev-command__group">` |
| Used inside | `evoq-ui:command` (EvCommand) |

## Example

```vue
<EvCommandGroup heading="Saran">
  <EvCommandItem label="Buat faktur baru" active />
  <EvCommandItem label="Cari pemasok" />
</EvCommandGroup>
```

_The exact snippet the playground simulator copies for this component (`playground/simulator-demos.ts`)._

Its default slot is built to hold `EvCommandItem` (`evoq-ui:command-item`).

## Props (1)

| Prop | Type | Default | Required | Description |
| --- | --- | --- | --- | --- |
| `heading` | `string` | — | no | The "Section Title" row the board draws at the top of each Section. |

## Slots (1)

| Slot | Description |
| --- | --- |
| `default` | The rows - `EvCommandItem` instances. |

## Events (0)

None.

## Composition

| Relation | Components |
| --- | --- |
| Built from | `evoq-ui:command-item` |
| Used by | — |
| Slot children | `evoq-ui:command-item` |

## Design guidance

From the Figma page **Command**, frame `4220:73205` (Command). These rules are binding. Names in them are Figma's - the Vue API is the tables above.

### When to use

- Give power users fast keyboard access to actions, navigation, and search across the app — triggered by ⌘K / Ctrl+K from anywhere.

### When not to use

- As the only way to reach an action. On simple apps with few commands — a visible menu or buttons are clearer.

### Do

- Group commands into clear sections. Show keyboard shortcuts (Kbd) for frequent actions. Keep labels action-first (verbs).

### Don't

- Don't bury critical actions only here. Don't list hundreds of flat commands without grouping or search.

## Contract

- Only the props, slots and events above exist on `EvCommandGroup`; anything not listed is not part of its API.
- An attribute that is not a prop (`class`, `id`, `aria-*`, `data-*`) goes to the root `<div class="ev-command__group">`.
- Import it by name from `evoq-ui`; styles come once from `evoq-ui/style.css`.
- Refer to it by id `evoq-ui:command-group` - the id stays the same if the component moves layer.

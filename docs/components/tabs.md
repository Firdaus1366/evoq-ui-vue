---
id: "evoq-ui:tabs"
name: "EvTabs"
tag: "ev-tabs"
layer: "molecule"
package: "evoq-ui"
entry: "evoq-ui"
import: "import { EvTabs } from 'evoq-ui'"
source: "src/molecules/tabs/EvTabs.vue"
summarySource: "figma"
partOf: null
children:
  - "evoq-ui:tab"
composes: []
usedBy: []
seeAlso:
  - "evoq-ui:navigation-menu"
figma:
  file: "hJ3XmwLptPtF4IFBDlZlpx"
  doc: "4100:63234"
  sets:
    - "Tabs (434:6135)"
keywords:
  - "tab bar"
  - "segmented"
  - "views"
---

<!-- GENERATED by scripts/build-docs.mjs - edit the sources it names, not this file. -->

# EvTabs

> Tabs let users switch between related views within the same context, showing one panel at a time. They come in a segmented (pill) style and a line (underline) style.

| Field | Value |
| --- | --- |
| ID | `evoq-ui:tabs` |
| Layer | Molecule (`src/molecules`) |
| Import | `import { EvTabs } from 'evoq-ui'` |
| Template tag | `<EvTabs>` or `<ev-tabs>` |
| Source | `src/molecules/tabs/EvTabs.vue` |
| Extra attributes go to | the root `<div class="ev-tabs">` |

## Example

```vue
<EvTabs model-value="ringkasan">
  <EvTab value="ringkasan">Ringkasan</EvTab>
  <EvTab value="dokumen">Dokumen</EvTab>
  <EvTab value="riwayat">Riwayat</EvTab>
</EvTabs>
```

_The exact snippet the playground simulator copies for this component (`playground/simulator-demos.ts`)._

Its default slot is built to hold `EvTab` (`evoq-ui:tab`).

## Props (3)

| Prop | Type | Default | Required | Description |
| --- | --- | --- | --- | --- |
| `modelValue` | `string \| number \| null` | `null` | no | — |
| `variant` | `'segmented'` \| `'line'` | `'segmented'` | no | — |
| `label` | `string` | — | no | Accessible name for the tab list. |

**Two-way binding:** `v-model`

## Slots (1)

| Slot | Description |
| --- | --- |
| `default` | — |

## Events (1)

| Event | Description |
| --- | --- |
| `update:modelValue` | — |

## Composition

| Relation | Components |
| --- | --- |
| Built from | — (renders no other Ev component) |
| Used by | — |
| Slot children | `evoq-ui:tab` |

## Design guidance

From the Figma page **Tabs**, frame `4100:63234` (Tabs). These rules are binding. Names in them are Figma's - the Vue API is the tables above.

### When to use

- Use to switch between related views or sections within one page or panel.
- Use when content is peer-level and only one view shows at a time.
- Use the segmented style for a small, fixed set of toggles or filters.
- Use the line style for primary, section-level navigation.

### When not to use

- Don't use for sequential steps  →  use a Stepper
- Don't use for page-to-page navigation  →  use a Nav menu (`evoq-ui:navigation-menu`)
- Don't use to filter many options  →  use Select / Chips
- Don't use when views are unrelated or hierarchical.

### Do

- Keep exactly one tab selected at all times
- Keep labels short (1–2 words) and parallel
- Keep the visible panel in sync with the active tab
- Limit the count so all tabs fit without scrolling

### Don't

- Open a new page or dialog from a tab
- Hide critical content behind a rarely-seen tab
- Use long labels that wrap or truncate
- Nest tabs inside tabs in the same view

## Contract

- Only the props, slots and events above exist on `EvTabs`; anything not listed is not part of its API.
- An attribute that is not a prop (`class`, `id`, `aria-*`, `data-*`) goes to the root `<div class="ev-tabs">`.
- Import it by name from `evoq-ui`; styles come once from `evoq-ui/style.css`.
- Refer to it by id `evoq-ui:tabs` - the id stays the same if the component moves layer.

---
id: "evoq-ui:label-item"
name: "EvLabelItem"
tag: "ev-label-item"
layer: "atom"
package: "evoq-ui"
entry: "evoq-ui"
import: "import { EvLabelItem } from 'evoq-ui'"
source: "src/atoms/label-item/EvLabelItem.vue"
summarySource: "code"
partOf: "evoq-ui:item"
children: []
composes: []
usedBy:
  - "evoq-ui:item"
seeAlso:
  - "evoq-ui:button"
figma:
  file: "hJ3XmwLptPtF4IFBDlZlpx"
  doc: "4282:80173"
  sets:
    - ".LabelItem (2050:996)"
keywords:
  - "title description"
  - "label caption"
  - "two line label"
---

<!-- GENERATED by scripts/build-docs.mjs - edit the sources it names, not this file. -->

# EvLabelItem

> A title with a caption under it, at one of three caption sizes. The block Item, Sidebar and Command all reuse for their text.

| Field | Value |
| --- | --- |
| ID | `evoq-ui:label-item` |
| Layer | Atom (`src/atoms`) |
| Import | `import { EvLabelItem } from 'evoq-ui'` |
| Template tag | `<EvLabelItem>` or `<ev-label-item>` |
| Source | `src/atoms/label-item/EvLabelItem.vue` |
| Extra attributes go to | the root `<div class="ev-label-item">` |
| Used inside | `evoq-ui:item` (EvItem) |

## Example

```vue
<EvLabelItem
  title="Title"
  description="Description"
/>
```

_The exact snippet the playground simulator copies for this component (`playground/simulator-demos.ts`)._

## Props (3)

| Prop | Type | Default | Required | Description |
| --- | --- | --- | --- | --- |
| `type` | `'default'` \| `'small'` \| `'extra-small'` | `'default'` | no | `Type` of the Figma `.LabelItem` set - it only changes the caption size. |
| `title` | `string` | — | no | — |
| `description` | `string` | — | no | Caption under the title. Figma names the node `Description` on Default and `Label` on the two small types; it is one slot here. |

## Slots (2)

| Slot | Description |
| --- | --- |
| `default` | — |
| `description` | — |

## Events (0)

None.

## Composition

| Relation | Components |
| --- | --- |
| Built from | — (renders no other Ev component) |
| Used by | `evoq-ui:item` |
| Slot children | — |

## Design guidance

From the Figma page **Item**, frame `4282:80173` (Item). These rules are binding. Names in them are Figma's - the Vue API is the tables above.

### When to use

- Use for rows in lists, menus, settings, or selection groups where each entry pairs a leading visual with a title, supporting text, and an optional action.

### When not to use

- Don't use for dense tabular data with many columns — use a Table. Don't use for a single standalone action — use a Button (`evoq-ui:button`).

### Do

- Keep one leading element per item and align trailing actions consistently down the list. Keep titles short and let the description carry detail.

### Don't

- Don't stack multiple leading visuals (avatar + image + icon) in one item. Don't crowd the row with several trailing actions — keep it to one.

## Contract

- Only the props, slots and events above exist on `EvLabelItem`; anything not listed is not part of its API.
- An attribute that is not a prop (`class`, `id`, `aria-*`, `data-*`) goes to the root `<div class="ev-label-item">`.
- Import it by name from `evoq-ui`; styles come once from `evoq-ui/style.css`.
- Refer to it by id `evoq-ui:label-item` - the id stays the same if the component moves layer.

---
id: "evoq-ui:line-chart"
name: "EvLineChart"
tag: "ev-line-chart"
layer: "organism"
package: "evoq-ui"
entry: "evoq-ui/charts"
import: "import { EvLineChart } from 'evoq-ui/charts'"
source: "src/charts/EvLineChart.vue"
summarySource: "code"
partOf: "evoq-ui:chart"
children: []
composes: []
usedBy: []
seeAlso:
  - "evoq-ui:bar-chart"
figma:
  file: "hJ3XmwLptPtF4IFBDlZlpx"
  doc: "4220:71982"
  sets:
    - "LineChart (3398:1486)"
keywords:
  - "line"
  - "trend"
  - "area chart"
  - "time series"
  - "graph"
---

<!-- GENERATED by scripts/build-docs.mjs - edit the sources it names, not this file. -->

# EvLineChart

> A line or area chart for trends over time, drawn in SVG from an array of records.

| Field | Value |
| --- | --- |
| ID | `evoq-ui:line-chart` |
| Layer | Organism (`src/organisms`) |
| Import | `import { EvLineChart } from 'evoq-ui/charts'` |
| Template tag | `<EvLineChart>` or `<ev-line-chart>` |
| Source | `src/charts/EvLineChart.vue` |
| Extra attributes go to | the root `<div class="ev-line-chart">` |
| Used inside | `evoq-ui:chart` (EvChart) |

Charts are a separate entry point. Install the optional peers first: `npm install @unovis/ts @unovis/vue`.

## Example

```vue
<EvLineChart
  :data='[{"bulan":"Jan","realisasi":42},{"bulan":"Feb","realisasi":61},{"bulan":"Mar","realisasi":38}]'
  category="bulan"
  :series='["realisasi"]'
/>
```

_The exact snippet the playground simulator copies for this component (`playground/simulator-demos.ts`)._

## Props (9)

| Prop | Type | Default | Required | Description |
| --- | --- | --- | --- | --- |
| `data` | `T[]` | — | yes | — |
| `category` | `keyof T & string` | — | yes | Key holding each row's position on the X axis. |
| `series` | `(keyof T & string)[]` | — | yes | One key per series. |
| `type` | `'line'` \| `'area'` | `'line'` | no | `area` fills the region beneath the trend, as the board's Area does. |
| `height` | `number` | `240` | no | — |
| `seriesLabels` | `string[]` | — | no | Names used in the tooltip. Falls back to the series keys. |
| `xLabel` | `string` | — | no | — |
| `yLabel` | `string` | — | no | — |
| `label` | `string` | — | no | Accessible description of what the chart shows. |

## Slots (0)

None.

## Events (0)

None.

## Composition

| Relation | Components |
| --- | --- |
| Built from | — (renders no other Ev component) |
| Used by | — |
| Slot children | — |

## Design guidance

From the Figma page **Chart**, frame `4220:71982` (Chart). These rules are binding. Names in them are Figma's - the Vue API is the tables above.

### When to use

- Visualize quantitative data — comparisons (bar), trends over time (line), or part-to-whole proportions (pie/doughnut). Wrap in the Chart container for a titled card.

### When not to use

- A few values better shown as text or a table. Pie charts for more than ~5 slices or precise comparisons — use a bar chart (`evoq-ui:bar-chart`) instead.

### Do

- Pick the chart type that fits the data question. Keep color usage consistent across series. Add value labels when exact figures matter.

### Don't

- Don't mix unrelated series in one chart. Don't use 3D or decorative effects. Don't omit axis labels on bar and line charts.

## Contract

- Only the props, slots and events above exist on `EvLineChart`; anything not listed is not part of its API.
- An attribute that is not a prop (`class`, `id`, `aria-*`, `data-*`) goes to the root `<div class="ev-line-chart">`.
- Import it by name from `evoq-ui/charts`; styles come once from `evoq-ui/style.css`.
- Refer to it by id `evoq-ui:line-chart` - the id stays the same if the component moves layer.

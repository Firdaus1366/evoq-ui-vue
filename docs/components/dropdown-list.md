---
id: "evoq-ui:dropdown-list"
name: "EvDropdownList"
tag: "ev-dropdown-list"
layer: "molecule"
package: "evoq-ui"
entry: "evoq-ui"
import: "import { EvDropdownList } from 'evoq-ui'"
source: "src/molecules/dropdown-list/EvDropdownList.vue"
summarySource: "code"
partOf: null
children:
  - "evoq-ui:dropdown-item"
composes:
  - "evoq-ui:input-search"
usedBy:
  - "evoq-ui:breadcrumb"
  - "evoq-ui:calendar"
seeAlso:
  - "evoq-ui:radio"
figma:
  file: "hJ3XmwLptPtF4IFBDlZlpx"
  doc: "4258:77060"
  sets:
    - "DropdownList (668:8852)"
keywords:
  - "select"
  - "menu"
  - "options"
  - "listbox"
  - "combobox"
  - "popover list"
---

<!-- GENERATED by scripts/build-docs.mjs - edit the sources it names, not this file. -->

# EvDropdownList

> A floating list panel - a listbox of options to choose from, or a menu of actions and links - with optional search, scrolling and an empty state.

| Field | Value |
| --- | --- |
| ID | `evoq-ui:dropdown-list` |
| Layer | Molecule (`src/molecules`) |
| Import | `import { EvDropdownList } from 'evoq-ui'` |
| Template tag | `<EvDropdownList>` or `<ev-dropdown-list>` |
| Source | `src/molecules/dropdown-list/EvDropdownList.vue` |
| Extra attributes go to | the root `<div class="ev-dropdown-list">` |

## Example

```vue
<EvDropdownList>
  <EvDropdownItem active>Semua status</EvDropdownItem>
  <EvDropdownItem>Menunggu persetujuan</EvDropdownItem>
  <EvDropdownItem>Disetujui</EvDropdownItem>
  <EvDropdownItem disabled>Dibatalkan</EvDropdownItem>
</EvDropdownList>
```

_The exact snippet the playground simulator copies for this component (`playground/simulator-demos.ts`)._

Its default slot is built to hold `EvDropdownItem` (`evoq-ui:dropdown-item`).

## Props (6)

| Prop | Type | Default | Required | Description |
| --- | --- | --- | --- | --- |
| `searchable` | `boolean` | `false` | no | Figma's `Has Search`: turns the list into the Combobox pattern. |
| `searchValue` | `string` | `''` | no | — |
| `searchPlaceholder` | `string` | `'Cari...'` | no | — |
| `scrollable` | `boolean` | `false` | no | Figma's `Has Scroll`: caps the visible height. Off by default - the doc says scrolling still works without it. Override the cap itself with `--ev-dropdown-list-max-height`. |
| `label` | `string` | — | no | Accessible name for the option list. |
| `menu` | `boolean` | `false` | no | A menu of actions or links rather than a listbox of options to choose from - the rows become menu items, and a row with `href` a real link. |

**Two-way binding:** `v-model:searchValue`

## Slots (2)

| Slot | Description |
| --- | --- |
| `default` | The options. |
| `empty` | Figma's `Not Found` variant. Render it instead of the options when a search returns nothing. |

## Events (1)

| Event | Description |
| --- | --- |
| `update:searchValue` | — |

## Composition

| Relation | Components |
| --- | --- |
| Built from | `evoq-ui:input-search` |
| Used by | `evoq-ui:breadcrumb`, `evoq-ui:calendar` |
| Slot children | `evoq-ui:dropdown-item` |

## Design guidance

From the Figma page **Dropdown List**, frame `4258:77060` (Dropdown List). These rules are binding. Names in them are Figma's - the Vue API is the tables above.

### When to use

- Let users pick from a set of options without taking up persistent space — select fields, filters, action menus, or autocomplete suggestions.

### When not to use

- For 2–3 options that fit inline → use Radio (`evoq-ui:radio`) or a segmented control. For app navigation → use a Menu or Nav, not a data dropdown.

### Do

- Keep labels short and scannable. Add search (Combobox) for long lists. Enable scroll past ~7–8 items. Show a clear Not Found state for empty results.

### Don't

- Don't pack dozens of items without search or scroll. Don't mix unrelated actions and data options. Don't auto-select an item on hover.

## Contract

- Only the props, slots and events above exist on `EvDropdownList`; anything not listed is not part of its API.
- An attribute that is not a prop (`class`, `id`, `aria-*`, `data-*`) goes to the root `<div class="ev-dropdown-list">`.
- Import it by name from `evoq-ui`; styles come once from `evoq-ui/style.css`.
- Refer to it by id `evoq-ui:dropdown-list` - the id stays the same if the component moves layer.
